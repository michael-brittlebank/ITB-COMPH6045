{% extends "layouts/main.twig" %}
{% block content %}
    <main id="page-admin-edit-product" class="grid-container row">
        <div class="col-sm-12 col-md-6 col-lg-4">
            <h1>Edit Product</h1>
            <a href="{{ path_for('admin-dashboard') }}" class="header-link"><i class="fa fa-chevron-left"></i> Back</a>
            <form id="admin-edit-product-form" class="standard-form">
                <label for="edit-product-title">Product Title</label>
                <input type="text" id="edit-product-title" value="{{ product.title }}"/>
                <label for="edit-product-price">Price</label>
                <input type="number" id="edit-product-price" step="0.01" min="0.01" value="{{ product.price }}"/>
                <label for="edit-product-url">Product Url</label>
                <input type="text" id="edit-product-url" value="{{ product.url }}"/>
                <input type="hidden" id="edit-product-id" value="{{ product.id }}" readonly="readonly"/>
                <label for="edit-product-category">Category</label>
                <select id="edit-product-category">
                    <option value="" disabled="disabled" {% if product.categoryId|length < 1 %}selected="selected"{% endif %}>No Category</option>
                    {% for category in categories%}
                        <option value="{{ category.id }}" {% if category.id == product.categoryId %}selected="selected"{% endif %}>{{ category.name|capitalize }}</option>
                    {% endfor %}
                </select>
                <button type="submit">Submit</button>
            </form>
        </div>
    </main>
{% endblock %}